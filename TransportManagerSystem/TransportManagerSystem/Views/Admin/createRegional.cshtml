@model TransportManagerSystem.Models.ModelsViews.Regional
@{
    ViewBag.Title = "createRegional";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>createRegional</h2>
@if(TempData["Warn"] != null)
{
    <div class="alert alert-danger" role="alert">
        Region has existed
    </div>
}
<form action="@Url.Action("createRegionalDB")" method="post" validate="true">
    <div class="form-group">
        <input type="text" name="Name" value="" id="" required/>
    </div>
    <button class="btn btn-primary" type="submit">Save</button>
</form>
